import React from 'react'
import Logo from "../assets/img/logo-qatar.png";

export const CartWidget = () => {
    return (
        <img src={Logo} alt="" width={50} />
    )
}

export default CartWidget